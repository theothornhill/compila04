program firsttest
begin
  var x : int;
  var y : float;

  struct Complex {
     Real : float;
     Imag : float
  };

  proc Main(a : int)
  begin
    struct C {
    One : float
    };
    var z : int
  in
  // if (5 + 5)
  // then
  // Main("halla")
  // fi;
  
  // // callThisFunction(5 + 5)
   end;
   struct L {
     MyNum : int
   }
end